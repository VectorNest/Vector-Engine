/* This file includes Provider configuration of the daemon process.
 * You (as an operator) need to rename this file to "providers.json" and fill the
 * empty fields inside of it. */

{
  /* Each provider information should be in different "tags" and associated
   * with a Provider class in src/index.ts*/

  /* This is a provider tag. Add as many as you need. */
  "main": {
    "providerWalletPrivateKey": "0x",
    "billingWalletPrivateKey": "0x",
    "operatorWalletPrivateKey": "0x"
  }
}
